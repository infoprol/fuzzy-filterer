type Product {
  id: String!
  isActive: Boolean
  price: Float
  name: String!
  about: String
  legacyId: String
  imageUrl: String
  tags: [String]
}

type Query {
  searchProducts(searchText: String, tags: [String]): [Product!]!
  getProduct(id: String!): Product
  getTags: [String!]!
}
