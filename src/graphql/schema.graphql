type Product {
  id: String
  isActive: Boolean
  price: Float
  name: String
  about: String
  legacyId: String
  imageUrl: String
  tags: [String]
}

type Query
type Mutation

extend type Query {
  getProduct(id: String!): Product
}
